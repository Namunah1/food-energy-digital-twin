# docs/figures/

One real figure exists from this project's work: the dominant causal
feedback loop diagram (produced during the Phase 2.5b causal
decomposition investigation, described in
`docs/architecture/CAUSAL_DECOMPOSITION.md` Section 11's ASCII rendering
— the original interactive SVG was generated in-conversation via a
visualization tool and was not saved as a static file to this
repository).

The publication task's full 13-figure / 8-diagram list (system
architecture, agent architecture, trade network, policy flow, causal
graph, simulation pipeline, optimization pipeline, frontend/backend/
deployment architecture, database schema, API flow) was **not**
produced at that breadth this session — see the final consolidation
report's "What was not completed" section for the explicit accounting.
Producing publication-quality static figures for all of these is real,
substantial remaining work, not a quick follow-up.
